# TronGrid API Key (required) — read-only blockchain access. Get from https://www.trongrid.io/
# This key is read-only: it authenticates RPC reads, it does NOT sign or move funds.
TRONGRID_API_KEY=

# Network: mainnet (default) or nile (testnet)
NETWORK=mainnet
